# Node modules
node_modules/

# Vite cache
.vite/

# Build output (GitHub Pages deploy uses Actions artifacts)
dist/

# Lockfiles (keep tracked for reproducible installs)
# package-lock.json
# yarn.lock
